inline x = %extern "x" {∀<a : %type %runtime %pointer> => a};

function getX = (∀<a : %type %runtime %pointer> => a) () {
	x
};

function id = <a : %type %runtime %pointer> (a)(x : a){
	x
};

function callId = <a : %type %runtime %pointer> (a)(x : a){
	id<a>(*)(x)
};


