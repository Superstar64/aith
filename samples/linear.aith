{
inline dup = Λ<b : %type %meta> {
	λ!(x : b) =>
	Λ<r : %type %meta> =>
	λ(f : !b -> !b -> r) =>
	f(!x)(!x)
};

inline main = Λ<a : %type %meta> {
	Λ<b : %type %meta> =>

	λ(f : a -> !b) =>
	λ(x : a) =>

	dup<b>(f(x))
};
}
