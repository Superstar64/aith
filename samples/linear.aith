%let !(dup : ∀<b : %type %runtime> => !b -> ∀<r : %type %runtime> => (!b -> !b -> r) -> r) = !Λ<b : %type %runtime> {
	λ!(x : b) =>
	Λ<r : %type %runtime> =>
	λ(f : !b -> !b -> r) =>
	f(!x)(!x)
};

Λ<a : %type %runtime> =>
Λ<b : %type %runtime> =>

λ(f : a -> !b) =>
λ(x : a) =>

dup<b>(f(x))
