%let !(dup : ∀<a : %type %meta> => !a -> ∀<r : %type %meta> => (!a -> !a -> r) -> r) =
!Λ<b : %type %meta> {
	λ!(x : b) =>
	Λ<r : %type %meta> =>
	λ(f : !b -> !b -> r) =>
	f(!x)(!x)
};

Λ<a : %type %meta> =>
Λ<b : %type %meta> =>

λ(f : a -> !b) =>
λ(x : a) =>

dup<b>(f(x))
