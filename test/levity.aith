idPolyPair<A : pretype<struct(pointer, 32bit word), false>>(x :: A) {
    x
}

idPolyUnion<A : pretype<union(pointer, 32bit word), false>>(x :: A) {
    x
}

idLevity<R : representation, A : pretype<R, false>> (x :: unique A*) {
    x
}

useId(ptr) {
    idPolyPair(ptr, 0)
}