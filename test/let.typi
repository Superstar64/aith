inc = x => x + 1;


~ world -> world ~
symbol main = os {
	nat = 1 : natural32;
	int = 2 : integer32;
	|os| = assert(nat.inc == 2, os);
	|os| = assert(int.inc == 3, os);
	os 
};

~ (& boolean, world &) -> world ~
assert = extern "assert";
