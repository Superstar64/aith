module:

function id = `\a : _type _runtime _data _real _pointer => \(x : a){
	_let y : a = x;
	y
};

function callId = `\a : _type _runtime _data _real _pointer => \(x : a){
	id`(a -> a)(id`(a))(x)
};
